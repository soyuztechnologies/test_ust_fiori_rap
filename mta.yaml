_schema-version: "3.2"
ID: travustabtravelproc
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: travustabtravelproc-destination-content
  type: com.sap.application.content
  requires:
  - name: travustabtravelproc-destination-service
    parameters:
      content-target: true
  - name: travustabtravelproc-repo-host
    parameters:
      service-key:
        name: travustabtravelproc-repo-host-key
  - name: travustabtravelproc-uaa
    parameters:
      service-key:
        name: travustabtravelproc-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: travustabtravelproc_html_repo_host
          ServiceInstanceName: travustabtravelproc-html5-srv
          ServiceKeyName: travustabtravelproc-repo-host-key
          sap.cloud.service: travustabtravelproc
        - Authentication: OAuth2UserTokenExchange
          Name: travustabtravelproc_uaa
          ServiceInstanceName: travustabtravelproc-xsuaa-srv
          ServiceKeyName: travustabtravelproc-uaa-key
          sap.cloud.service: travustabtravelproc
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: travustabtravelproc-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: travustabtravelproc-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - travustabtravelproc.zip
      name: travustabtravelproc
      target-path: resources/
- name: travustabtravelproc
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: travustabtravelproc-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: travustabtravelproc-destination-service
    service-plan: lite
- name: travustabtravelproc-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: travustabtravelproc-xsuaa-srv
    service-plan: application
- name: travustabtravelproc-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: travustabtravelproc-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
